export declare const findChromeInstallation: () => Promise<string | undefined>;
/**
 * Run callback with modified CHROME_PATH env variable if it has been pointed to
 * the ".app" directory instead of the executable binary for Darwin.
 */
export declare const withNormalizedChromePathForDarwin: <T>(callback: () => T) => Promise<T>;
